import React from "react";
import { Heading, HeroContainer, Text } from "./Hero.style";

const Hero = () => {
  return (
    <HeroContainer>
      <Heading>Stake ASVA to Join IDO lunchpad</Heading>
      <Text>
        will be releasing a total of three phases including LP tokens and
        multicurrency tokens.
      </Text>
    </HeroContainer>
  );
};

export default Hero;
